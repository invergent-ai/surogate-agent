"""Human-in-the-loop (HITL) support for surogate-agent.

Provides LangChain tools that pause a LangGraph session and route a task
to a different user, resuming the session once the user responds.
"""
